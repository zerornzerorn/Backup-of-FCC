function sumFibs(num) {
  
  var a=1;
  var b=1;  
  var sum = a+b;  
  while( b+a <= num){    
    b = a+b;    
    a = b-a;    
    if(b%2!==0){
      sum= sum +b;
     }  
  }
  if(num === 1){return 1;}
  else return sum;
}

sumFibs(4);
