function repeat(str, num) {
  // repeat after me
  if( num< 0){
    
    return "";
  }
  
  var temp='';
  
  for ( var i = 0; i <num; i++ ){
    
    temp += str;
    
  }
  
  return temp;
}

repeat("abc", 3);
